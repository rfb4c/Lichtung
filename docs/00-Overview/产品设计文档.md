# 林间空地 Lichtung - 产品设计文档

> **产品名称**：林间空地 Lichtung
> **命名寓意**：海德格尔哲学中的"澄明之境"——在算法密林中显现真实的空地
> **项目定位**：Demo 原型，用于向导师展示干预机制的可行性
> **当前状态**：数据写死（手动准备 mock 数据），专注验证干预效果
> **未来扩展**：可扩展为自动化数据采集系统（NewsAgent）
> **受众**：导师评审 + 下一阶段的实施团队
> **文档版本**：v3.0
> **最后更新**：2026-02-28
> **变更说明**：v3.0 添加 Path C（交叉身份可视化）完整设计；明确 Demo 定位（数据写死）；区分当前版本与未来扩展

* * *

## 1. 背景与研究动机

### 1.1 核心问题

**多数错觉（Majority Illusion）**：

* Lerman et al. (2016)在*PLOS ONE*发现：社交网络的异配结构中，仅20%的节点活跃即可导致60-70%的节点产生"该观点为多数"的错误感知
* 算法推荐系统放大这一效应：高参与度内容（点赞、转发、评论）被过度呈现，用户误以为"我看到的=真实分布"

**现实影响**：

* 用户基于偏差样本形成对"外群体立场"的误判
* 夸大对方敌意程度（Moore-Berg et al., 2020）
* 形成"大家都很激进"的错觉

### 1.2 干预假设

**如果**在用户即将接触评论区极端观点之前，先展示真实的公众态度分布数据，**那么**用户对意见分布的估计将更准确，从而降低感知极化——具体表现为修正用户对"对方阵营"极端程度的过度估计。

**注意**：干预目标不是改变用户立场，也不是利用多数压力施压少数派，而是修正用户对对立群体内部异质性的感知。

### 1.3 设计目标

**不是**：

* ❌ 改变用户的政治立场
* ❌ 推荐"更好"的内容
* ❌ 做完整的新闻产品
* ❌ 用多数人的立场施压少数派（从众压力工具）

**而是**：

* ✅ 让用户意识到"我看到的评论 ≠ 真实的公众态度分布"
* ✅ 修正用户对对方阵营极端程度的过度估计
* ✅ 校准用户对意见分布的感知
* ✅ 验证"分布可视化"的干预效果

* * *

## 2. 产品设计方案

### 2.1 核心功能

**Feed流 + 评论区默认曝光 + 独立数据按钮**

**设计理念**：

* 默认曝光（Default Exposure）：不阻断用户操作，但将权威分布数据放置在用户必经的视觉路径上
* 体验式学习（Experiential Learning）：让用户自己发现评论区极端样本与真实分布的差异
* 认知失调（Cognitive Dissonance）：通过直方图与评论区的并置，触发"评论区的声音 vs 真实分布"的冲击

**为什么干预点是评论区而非报道本身**：

* 看报道时，用户接收的是事实信息，尚未形成对"对方阵营"的感知
* 评论区才是感知极化的制造现场——用户在此看到对方阵营最激进、最高赞的发言，并将极端样本当作对方的全貌
* 精确干预时机：用户已产生"想看看别人怎么想"的意图，但尚未被极端评论锚定认知的窗口

### 2.2 用户旅程

```
用户打开网站
    ↓
滑动Feed流，浏览多个事件的报道
    ↓
看到一篇关于堕胎议题的报道，卡片上有轻量议题标签角标
    ↓
[路径A：被动干预 — 主干路径]
点击「💬 评论」按钮，想看看别人怎么想
    ↓
评论区展开，顶部固定显示 Pew 调查直方图
（不可折叠、不可关闭，自然占据评论区第一屏）
    ↓
用户看到：大部分人态度温和，极端立场是少数
    ↓
向下滚动，看到评论区的极端发言
    ↓
[认知冲击] "评论区吵得这么凶，但刚才数据显示大部分人其实没那么极端"
    ↓
[自我发现] "评论区的声音不代表真实的民意分布"

---

[路径B：主动探索 — 辅助路径]
用户对某议题好奇，点击卡片上的「📊 公众态度数据」按钮
    ↓
仅展开直方图，不显示评论
    ↓
用户主动查看该议题的公众态度分布
```

### 2.3 Path C：交叉身份可视化

**干预目标**：通过展示评论者的非政治身份标签，让用户在自然阅读过程中**主动发现**与政治立场对立的评论者之间存在非政治层面的共同身份。

**核心机制**：交叉身份（Cross-cutting Identity）

> 当两个政治立场对立的人发现彼此共享某个非政治身份（同为父母、同为退伍军人、同样在照顾病重的家人），内外群体的边界随之软化。这一机制的关键在于**自我发现**——系统仅提供信息，由用户自己建立连接，而非系统主动告知「你们有共同点」。

**设计原则**：
- ✅ **自然发现**：系统只呈现标签，由用户自己发现重合
- ✅ **非政治属性**：标签聚焦生活处境、人生经历，不涉及政治立场
- ✅ **完全自愿**：不强制填写，不做交集检测

**Path C 用户旅程**：

```
用户注册完成后
    ↓
进入「身份档案」引导步骤（一次性）
    ↓
系统引导："Let your comments carry context — help others
            understand who you are beyond your opinions."
    ↓
用户选择填写或跳过：
    - 第一层：家庭处境（Father, Mother, Caregiver...）
    - 第二层：人生经历（Veteran, Immigrant...）
    - 第三层：社会角色（Educator, Healthcare worker...）
    - 第四层：生活方式（Pet owner, Cooking...）
    ↓
用户可选填写个人叙事（如："Father of two girls, 8 and 12."）
    ↓
用户在评论区发表评论
    ↓
其他用户看到评论时，在评论卡片上看到标签：
┌────────────────────────────────────┐
│  [头像]  Username                 │
│  ← Father · Veteran · Cooking →  │  ← 横向可滑动 chips
│                                    │
│  "I think background checks...    │
│   be more thorough..."             │
└────────────────────────────────────┘
    ↓
[自我发现场景]
对立立场的用户A看到用户B的评论和标签
    ↓
用户A内心："他也是父亲？我也有两个孩子..."
    ↓
用户A点击「Father」标签，展开个人叙事
    ↓
看到："Father of two girls, 8 and 12. Trying to raise
      them in a world that feels increasingly divided."
    ↓
[认知软化] "虽然我们在这个议题上立场不同，但我们都是父亲，
            都在担心孩子的未来"
    ↓
内外群体边界软化，感知极化降低
```

**Path C 的四层标签体系**：

| 层级 | 标签类型 | 示例 | 自然流露程度 |
|------|---------|------|-------------|
| 第一层 | 家庭处境 | Father, Mother, Caregiver for a sick family member | 最高 |
| 第二层 | 人生经历 | Veteran, Immigrant, First-generation college student | 高 |
| 第三层 | 社会角色 | Educator, Healthcare worker, Small business owner | 中 |
| 第四层 | 生活方式 | Pet owner, Cooking, Outdoor recreation | 低 |

**关键设计决策**：
- **触发条件**：仅依据「对方有无档案」，而非「双方是否有交集」
- **不做高亮**：不向用户提示「你们有共同点」，完全由用户自己发现
- **不做排序**：评论排序保持中立，不因身份交集做任何权重调整

详见 [Path-C 交叉身份设计规范](../02-Path-C-交叉身份/设计规范.md)。

### 2.4 Path B + Path C 的协同关系

| 干预路径 | 干预层次 | 干预对象 | 协同方式 |
|---------|---------|---------|---------|
| **Path B** | 宏观：校正对整体民意分布的感知 | 用户对「公众态度分布」的估计 | 评论区顶部直方图 |
| **Path C** | 微观：校正对对立群体「铁板一块」的感知 | 用户对「对立个体」的刻板印象 | 评论卡片标签 |

**双层干预效果**：
- Path B：「对方没那么极端」（数据纠正）
- Path C：「对方其实和你一样是个真实的人」（人性化连接）
- 两者并置：既校准理性认知，又软化情感边界

* * *

## 3. UI/UX 详细规范

### 3.1 设计系统：模仿Twitter/X

**核心原则**：直接采用Twitter的成熟UI模式，用户零学习成本

**布局**：

```
┌─────────────────────────────────────┐
│ 🌲 林间空地 Lichtung              │ ← 顶部导航
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐  │
│  │ [堕胎权利]                  │  │ ← 议题标签角标
│  │ 某州女性因无法堕胎被迫生产   │  │
│  │ 后去世，引发全国讨论...      │  │
│  │                             │  │
│  │ 🔗 来源：AP News • 3小时前  │  │
│  │                             │  │
│  │ 💬 评论(234)  📊 公众态度   │  │ ← 双按钮
│  └─────────────────────────────┘  │
│                                     │
│  ┌─────────────────────────────┐  │
│  │ [枪支管控]                  │  │
│  │ 国会再次就背景调查法案...    │  │
│  │                             │  │
│  │ 💬 评论(89)   📊 公众态度   │  │
│  └─────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

**Twitter风格规范**：

**颜色方案**：

* 背景：`#FFFFFF`（白色）
* 卡片背景：`#FFFFFF`
* 边框：`#EFF3F4`（浅灰）
* 悬停背景：`#F7F9F9`
* 主色调：`#1D9BF0`（Twitter蓝）

**字体**：

* 标题：`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`
* 大小：标题15px，正文15px，时间13px
* 行高：1.5

**报道卡片（完全模仿Tweet卡片）**：

* 宽度：598px（Twitter标准）
* 内边距：12px 16px
* 边框：1px solid #EFF3F4
* 圆角：无（Twitter现在用的是直角）
* 底部边框：1px分隔线
* 悬停：背景变为 #F7F9F9

**标签样式（模仿Twitter的标签）**：

* 样式：圆角矩形，小号字体
* 外媒：`background: #E8F5FE; color: #1D9BF0`
* 国内：`background: #FFF4E6; color: #F57C00`
* 议题标签：`background: #F3E8FD; color: #7C3AED`
* 字号：13px
* 内边距：2px 8px
* 圆角：12px

### 3.2 卡片操作按钮

卡片底部包含两个操作按钮，仅在报道匹配到预设议题时显示「📊 公众态度」按钮：

| 按钮 | 功能 | 显示条件 |
|---|---|---|
| 💬 评论(N) | 展开评论区（顶部含直方图） | 始终显示 |
| 📊 公众态度 | 仅展开直方图，不显示评论 | 仅匹配议题时显示 |

**按钮样式**：

* 字号：13px
* 颜色：`#536471`（Twitter 次级文字色）
* 悬停：对应主题色高亮
* 间距：按钮间距 24px

### 3.3 评论区展开态（主干干预路径）

用户点击「💬 评论」按钮后，评论区在卡片下方内联展开（非跳转新页面）。

**结构**：

```
┌──────────────────────────────────┐
│ [报道卡片内容...]                │
│ 💬 评论(234)    📊 公众态度      │
├──────────────────────────────────┤
│                                  │
│ ┌─ 📊 公众对此议题的真实态度 ──┐ │
│ │                              │ │
│ │ 根据 Pew Research 2024 年    │ │
│ │ 调查，美国公众对堕胎合法性   │ │
│ │ 的态度分布如下：              │ │
│ │                              │ │
│ │  完全非法       ██  8%       │ │
│ │  多数情况非法   ██████ 27%   │ │
│ │  多数情况合法   ████████ 36% │ │
│ │  完全合法       █████ 19%    │ │
│ │                              │ │
│ │  数据来源：Pew Research 2024 │ │
│ └──────────────────────────────┘ │
│                                  │
│ 🔥 热门评论                      │
│ ┌──────────────────────────────┐ │
│ │ "这就是为什么必须恢复罗伊案！"│ │
│ │ ❤️ 1.2k                      │ │
│ ├──────────────────────────────┤ │
│ │ "杀婴就是杀婴，没有例外..."   │ │
│ │ ❤️ 856                       │ │
│ ├──────────────────────────────┤ │
│ │ ...更多评论                  │ │
│ └──────────────────────────────┘ │
│                                  │
├──────────────────────────────────┤
│ [下一条报道...]                  │
└──────────────────────────────────┘
```

**直方图组件规格**：

* 位置：评论区最顶部，固定不可折叠、不可关闭
* 宽度：与卡片等宽
* 背景：`#F8F9FA`（极浅灰，与评论区白色区分）
* 边框：1px solid `#E8E8E8`
* 圆角：8px
* 内边距：16px
* 底部间距：与评论列表间距 12px

**直方图视觉规范**：

* 图表类型：水平分布直方图（Horizontal Bar Chart）
* X 轴：受访者百分比
* Y 轴：4–7 级态度刻度（档数和标签由原始数据源决定）
* 柱状条高度：24px，间距 8px
* 柱状条圆角：右侧 4px
* 百分比标签：柱状条右侧，13px，`#536471`
* 视觉重点：以面积（柱子长度）对比自然凸显"温和派/中间档占多数"的事实

**色彩方案（暖色系非政治中性色，参考 Pew Research 图表风格）**：

* 设计原则：使用暖色调中性色，各档位可区分但不暗示阵营对立或政治属性
* 严禁使用红/蓝（美国政党色）、红/绿（对立暗示）等带有政治属性的配色
* 推荐色板（以 4 档为例）：
  * 档位 1：`#D4A574`（沙棕，Sand）
  * 档位 2：`#C4956A`（焦糖，Caramel）
  * 档位 3：`#9C8AA5`（灰紫，Mauve）
  * 档位 4：`#7B6B8A`（深灰紫，Plum Grey）
* 档数可变时（5–7 档）：在上述色板基础上等距插值，保持暖色调中性风格
* 配色逻辑：量表两端分属不同色相（暖棕 → 灰紫），中间档位为过渡色，避免任何一端的颜色被解读为"好"或"坏"

**桥接文案（必须）**：

直方图前必须配有说明文案，消除与报道之间的突兀感。

格式：
> 「根据 [数据来源] [年份] 的调查，美国公众对[议题名称]的态度分布如下：」

### 3.4 独立数据展开态（辅助探索路径）

用户点击「📊 公众态度」按钮后，仅在卡片下方展开直方图组件，不显示评论。

复用与 3.3 相同的直方图组件，区别仅在于不附带评论列表。

### 3.5 Feed 卡片议题标签角标

报道卡片上保留轻量视觉指示（议题标签角标，如「堕胎权利」「枪支管控」），告知用户此报道已匹配到预设议题。

* 仅在报道匹配议题时显示
* 匹配失败时不显示角标和「📊 公众态度」按钮

### 3.6 Path C UI 规格：评论卡片身份标签

#### 3.6.1 评论卡片结构（含 Path C 标签）

```
┌────────────────────────────────────────┐
│  [头像]  Username                      │
│  ← Father · Veteran · Cooking →       │  ← 横向可滑动 chips
│                                        │
│  "I think background checks should... │
│   be more thorough, but banning       │
│   assault rifles goes too far."       │
│                                        │
│  ❤️ 42  💬 5  ⏰ 2小时前               │
└────────────────────────────────────────┘
```

**触发条件**：
- 评论者已填写身份标签档案 → 显示标签行
- 评论者未填写档案 → 不显示标签行，卡片与普通评论无异

#### 3.6.2 标签 Chip 样式

- **字号**：12px
- **内边距**：4px 10px
- **圆角**：12px
- **背景**：`#F7F9F9`（Twitter 悬停背景色，保持轻量）
- **文字**：`#536471`（Twitter 次级文字色）
- **有叙事标识**：chip 右下角添加 `•` 点状指示

**示例**：

```
┌─────────────┐  ┌───────────────┐  ┌──────────────┐
│  Father     │  │  Veteran •    │  │  Cooking     │
└─────────────┘  └───────────────┘  └──────────────┘
                      ↑ 有叙事标识
```

#### 3.6.3 叙事展开态

点击有叙事的 chip 后，在 chip 下方内联展开叙事文字，不跳转新页面，不使用 modal。

```
[Veteran ▾]
  "Served in Afghanistan 2010-2012. Lost three buddies
   there. Still working through it."
```

**交互**：
- 点击有叙事的 chip → 展开叙事
- 再次点击 → 收起叙事
- 点击其他 chip → 收起当前叙事，展开新叙事

#### 3.6.4 标签档案填写（注册引导）

用户完成注册后，进入「身份档案」引导步骤（一次性）。

**引导文案**：
> "Let your comments carry context — help others understand who you are beyond your opinions."

**界面布局**（Mobile 优先）：

```
┌──────────────────────────────────────┐
│  Help others understand who you are  │
│                                       │
│  ○ 第一层：家庭处境                   │
│    ☑ Father                          │
│    ☐ Mother                          │
│    ☐ Caregiver for a sick family... │
│                                       │
│  ○ 第二层：人生经历                   │
│    ☑ Veteran                         │
│      [填写叙事（可选）]               │
│      Served in Afghanistan 2010...   │
│                                       │
│  [跳过] [保存并继续]                 │
└──────────────────────────────────────┘
```

**填写要求**：
- 第一层（家庭处境）：强烈鼓励，引导中优先展示
- 第二层（人生经历）：鼓励填写
- 第三层（社会角色）：可选
- 第四层（生活方式）：可选
- **跳过按钮**：始终允许，不设硬性强制

* * *

## 4. 技术架构

### 4.1 技术栈选择

**推荐：Vite + React + TypeScript**

**理由**：

* ✅ Vite：极快的开发服务器
* ✅ React：组件化，易于状态管理
* ✅ TypeScript：类型安全，减少bug
* ✅ 轻量：不需要Angular的复杂性

**替代方案**：

* Vite + Vue 3（如果更熟悉Vue）
* 纯HTML + Vanilla JS（如果极简化）

### 4.2 项目结构

```
majority-illusion-demo/
├── src/
│   ├── components/
│   │   ├── FeedItem.tsx            # 报道卡片（含操作按钮）
│   │   ├── CommentSection.tsx      # 评论区（含顶部直方图 + 评论列表）
│   │   ├── DistributionChart.tsx   # 直方图组件（被评论区和独立入口复用）
│   │   └── TopicBadge.tsx          # 议题标签角标
│   ├── data/
│   │   ├── reports.json            # 报道数据
│   │   ├── topics.json             # 议题库（中层）
│   │   ├── polling.json            # 民调分布数据（Pew/ANES）
│   │   └── comments.json           # 模拟评论数据
│   ├── types/
│   │   └── index.ts                # TypeScript类型定义
│   ├── App.tsx                     # 主组件
│   └── main.tsx                    # 入口
├── public/
├── package.json
└── vite.config.ts
```

### 4.3 数据结构

```typescript
// 议题库（中层）
interface Topic {
  id: string;                    // "us-abortion"
  name: string;                  // "堕胎权利"
  tagKeywords: string[];         // ["abortion", "roe", "reproductive rights"]
}

// 民调分布数据（支持 4–7 级可变量表）
interface PollingData {
  id: string;
  topicId: string;               // 关联到 Topic.id
  source: string;                // "Pew Research Center"
  surveyYear: number;            // 2024
  scaleLabels: string[];         // ["完全非法", "多数情况非法", "多数情况合法", "完全合法"]
  distribution: number[];        // [8, 27, 36, 19]  与 scaleLabels 等长
  bridgingText: string;          // 桥接文案模板
}

// 报道
interface Report {
  id: string;
  topicId?: string;              // 可选，关联到 Topic.id（匹配成功时有值）
  title: string;
  summary: string;
  source: string;                // "AP News", "BBC", "CNN" 等
  sourceUrl: string;             // 原文链接（点击卡片跳转）
  publishedAt: string;           // ISO 时间
}

// 模拟评论
interface Comment {
  id: string;
  topicId: string;               // 关联到 Topic.id
  author: string;
  content: string;
  likes: number;
}
```

**示例数据**：

```json
{
  "topics": [
    {
      "id": "us-abortion",
      "name": "堕胎权利",
      "tagKeywords": ["abortion", "roe", "reproductive rights", "dobbs"]
    }
  ],
  "polling": [
    {
      "id": "poll_001",
      "topicId": "us-abortion",
      "source": "Pew Research Center",
      "surveyYear": 2024,
      "scaleLabels": ["完全非法", "多数情况非法", "多数情况合法", "完全合法"],
      "distribution": [8, 27, 36, 19],
      "bridgingText": "根据 Pew Research 2024 年的调查，美国公众对堕胎合法性的态度分布如下："
    }
  ],
  "reports": [
    {
      "id": "rp_001",
      "topicId": "us-abortion",
      "title": "某州女性因无法堕胎被迫生产后去世，引发全国讨论",
      "summary": "该事件再次将堕胎权利推上舆论焦点...",
      "source": "AP News",
      "sourceUrl": "https://apnews.com/example",
      "publishedAt": "2024-05-15T10:00:00Z"
    }
  ],
  "comments": [
    {
      "id": "cm_001",
      "topicId": "us-abortion",
      "author": "user_anon_1",
      "content": "这就是为什么必须恢复罗伊案！任何限制都是对女性的压迫！",
      "likes": 1200
    },
    {
      "id": "cm_002",
      "topicId": "us-abortion",
      "author": "user_anon_2",
      "content": "杀婴就是杀婴，没有例外。这个国家的道德已经沦丧。",
      "likes": 856
    }
  ]
}
```

> **说明**：模拟评论应故意选取极端立场的高赞内容，以制造与直方图分布之间的对比效应——这正是干预的核心机制。

* * *

## 5. 实施步骤

### Phase 1: 数据准备（半天）

**任务**：

1. 确认 Pew 原始调查报告中各档百分比数据（手动验证，不依赖 AI 生成）
2. 将已验证的民调数据结构化录入 `polling.json`
3. 生成模拟报道和模拟评论数据

**模拟数据生成策略**：

* 报道：基于真实新闻标题改写，每个议题 10-20 篇
* 评论：故意偏向极端，高赞评论以激进立场为主，模拟真实评论区的选择偏差
* 民调数据：严格使用 Pew/ANES 原始数据，不编造

### Phase 2: 前端开发（2天）

**Day 1**：

* [ ] 搭建 Vite + React 项目
* [ ] 实现 `FeedItem` 组件（含双按钮：评论 + 公众态度）
* [ ] 实现 `TopicBadge` 组件（议题标签角标）
* [ ] 实现基本的滚动 Feed
* [ ] 实现卡片点击跳转外部新闻源

**Day 2**：

* [ ] 实现 `DistributionChart` 组件（N 档可变直方图）
* [ ] 实现 `CommentSection` 组件（顶部直方图 + 评论列表）
* [ ] 集成评论区展开/收起逻辑
* [ ] 集成独立数据按钮展开逻辑
* [ ] 样式美化

### Phase 3: 测试与优化（1天）

* [ ] 内部测试（自己试用完整流程）
* [ ] 验证直方图在不同档数（4档/5档/7档）下的显示效果
* [ ] 验证评论区与直方图的并置对比效果
* [ ] 性能优化
* [ ] 准备部署

* * *

## 6. 关键代码示例

### 6.1 FeedItem 组件

```tsx
import { useState } from 'react';
import { Report, Topic } from '../types';
import CommentSection from './CommentSection';
import DistributionChart from './DistributionChart';
import TopicBadge from './TopicBadge';

interface Props {
  report: Report;
  topic?: Topic;        // 匹配到的议题（可能无）
}

export default function FeedItem({ report, topic }: Props) {
  const [showComments, setShowComments] = useState(false);
  const [showDataOnly, setShowDataOnly] = useState(false);

  const hasTopic = !!topic;

  return (
    <div className="feed-item">
      {/* 议题标签角标 */}
      {hasTopic && <TopicBadge topic={topic} />}

      {/* 报道内容（点击跳转外部新闻源） */}
      <a href={report.sourceUrl} target="_blank" rel="noopener noreferrer">
        <h3>{report.title}</h3>
        <p>{report.summary}</p>
      </a>

      <div className="meta">
        🔗 来源：{report.source} • {formatTime(report.publishedAt)}
      </div>

      {/* 操作按钮 */}
      <div className="actions">
        <button onClick={() => {
          setShowComments(!showComments);
          setShowDataOnly(false);
        }}>
          💬 评论
        </button>

        {hasTopic && (
          <button onClick={() => {
            setShowDataOnly(!showDataOnly);
            setShowComments(false);
          }}>
            📊 公众态度
          </button>
        )}
      </div>

      {/* 评论区展开（含顶部直方图） */}
      {showComments && (
        <CommentSection topicId={report.topicId} />
      )}

      {/* 独立数据展开（仅直方图） */}
      {showDataOnly && (
        <DistributionChart topicId={report.topicId} />
      )}
    </div>
  );
}
```

### 6.2 DistributionChart 组件（N 档可变直方图）

```tsx
import { PollingData } from '../types';

interface Props {
  pollingData: PollingData;
}

// 生成暖色系中性色梯度：从沙棕到灰紫，档数可变
function getBarColors(count: number): string[] {
  // 色板锚点：暖棕端 → 灰紫端
  const warmEnd  = [212, 165, 116]; // #D4A574 Sand
  const coolEnd  = [123, 107, 138]; // #7B6B8A Plum Grey
  return Array.from({ length: count }, (_, i) => {
    const ratio = count === 1 ? 0.5 : i / (count - 1);
    const r = Math.round(warmEnd[0] + (coolEnd[0] - warmEnd[0]) * ratio);
    const g = Math.round(warmEnd[1] + (coolEnd[1] - warmEnd[1]) * ratio);
    const b = Math.round(warmEnd[2] + (coolEnd[2] - warmEnd[2]) * ratio);
    return `rgb(${r}, ${g}, ${b})`;
  });
}

export default function DistributionChart({ pollingData }: Props) {
  const maxPercent = Math.max(...pollingData.distribution);
  const colors = getBarColors(pollingData.scaleLabels.length);

  return (
    <div className="distribution-chart">
      {/* 桥接文案 */}
      <p className="bridging-text">{pollingData.bridgingText}</p>

      {/* N 档直方图 */}
      <div className="bars">
        {pollingData.scaleLabels.map((label, index) => (
          <div key={index} className="bar-row">
            <span className="bar-label">{label}</span>
            <div className="bar-track">
              <div
                className="bar-fill"
                style={{
                  width: `${(pollingData.distribution[index] / maxPercent) * 100}%`,
                  backgroundColor: colors[index],
                }}
              />
            </div>
            <span className="bar-percent">
              {pollingData.distribution[index]}%
            </span>
          </div>
        ))}
      </div>

      {/* 数据来源标注 */}
      <p className="source-note">
        数据来源：{pollingData.source} ({pollingData.surveyYear})
      </p>
    </div>
  );
}
```

### 6.3 CommentSection 组件

```tsx
import DistributionChart from './DistributionChart';
import { Comment, PollingData } from '../types';

interface Props {
  pollingData?: PollingData;    // 匹配到议题时有值
  comments: Comment[];
}

export default function CommentSection({ pollingData, comments }: Props) {
  return (
    <div className="comment-section">
      {/* 顶部固定直方图（不可折叠、不可关闭） */}
      {pollingData && (
        <div className="chart-pinned">
          <DistributionChart pollingData={pollingData} />
        </div>
      )}

      {/* 评论列表 */}
      <div className="comments-list">
        <h4>🔥 热门评论</h4>
        {comments.map(comment => (
          <div key={comment.id} className="comment-item">
            <p>{comment.content}</p>
            <span className="comment-likes">❤️ {comment.likes}</span>
          </div>
        ))}
      </div>
    </div>
  );
}
```

* * *

## 7. 设计原则

### 7.1 默认曝光（Default Exposure）

**核心**：不阻断用户操作，不要求额外动作，但将权威数据放在用户必经的视觉路径上。研究表明即使用户仅快速扫视，锚定效应仍会在后续阅读评论时起作用。

**不做**：

* ❌ 弹窗警告"你的看法可能有偏差"
* ❌ 将直方图作为查看评论的强制门槛（会扼杀评论参与度）
* ❌ 默认折叠直方图（等同于自愿入口，曝光率过低）
* ❌ 阻断用户的阅读流程

**而做**：

* ✅ 评论区顶部固定展示直方图，不可折叠、不可关闭
* ✅ 用户向下滚动即可查看评论，无任何阻断
* ✅ 另设独立数据按钮供主动探索
* ✅ 保持阅读的流畅性

### 7.2 体验式学习

**核心**：让用户自己发现评论区极端样本与真实分布的矛盾，而非被动告知

**不加**：

* ❌ "评论区夸大了反对声音"
* ❌ "你的判断可能有误"
* ❌ 任何带有说教意味的文案

**依赖**：

* ✅ 直方图与极端评论的并置产生的自然对比
* ✅ 用户的自我意识："评论区吵得这么凶，但数据显示大部分人其实没那么极端？"
* ✅ 认知失调的自然触发

### 7.3 学术严谨性

**数据真实性**：

* 仅使用预先验证的外部权威民调数据（Pew Research / ANES）
* 直方图档数由原始数据源决定（4–7 级），不人为增减
* 严禁使用平台内部数据、用户投票等生成图表
* 严禁使用"支持 vs 反对"二元数据

**单一变量**：

* 不叠加其他干预（如情绪过滤、去标签）
* 保证测量的是"分布可视化"的效果

**可测量**：

* 记录评论按钮点击率 vs 独立数据按钮点击率
* 记录用户在直方图区域的停留时间
* 前后测：用户对分布的估计准确度
* A/B对照：有/无直方图的评论区对比

* * *

## 8. 当前 Demo 定位与未来扩展

### 8.1 当前 Demo 版本（v0.2.x）

**核心特征：数据写死，专注干预验证**

| 模块 | 当前实现 | 数据来源 |
|------|---------|---------|
| **报道内容** | 手动准备 JSON | 基于真实新闻改写 |
| **民调数据（Path B）** | 手动验证后录入 | **严格使用 Pew Research 原始数据** |
| **身份标签（Path C）** | 手动准备种子数据 | 10 个 mock 用户，手动分配标签 |
| **评论数据** | 手动编写极端评论 | 故意偏向极端，制造对比效应 |

**为什么数据写死？**
- ✅ **快速验证**：专注干预机制本身，不在数据采集上耗时
- ✅ **质量可控**：民调数据严格验证，评论数据精心设计
- ✅ **适合演示**：向导师展示干预效果，无需完整数据管线

**不实现的功能**：
- ❌ NewsAgent 自动新闻采集
- ❌ 自动议题标签分类
- ❌ 定时数据更新

### 8.2 未来扩展：自动化数据采集

**扩展方向**：从 Demo 升级为可持续运行的系统

#### 8.2.1 NewsAgent 集成

详见 [News-Agent 设计规范](../03-News-Agent/设计规范.md)。

- **自动报道采集**：
  - 定时爬取新闻网站（AP News, BBC, CNN）
  - 使用 LLM 提取标题、摘要、立场
  - 输出结构化 JSON
- **自动议题标签**：
  - 基于预定义议题库关键词匹配
  - 或使用 LLM 语义分类
- **自动导入 Supabase**：
  - Node 脚本调用 Supabase API 批量插入
  - 定时任务（每日/每小时）

#### 8.2.2 民调数据版本管理

- **追踪 Pew Research 更新**：
  - 监控 Pew 官网新发布的调查报告
  - 手动或半自动更新 `polling_data` 表
- **数据版本控制**：
  - 保留历史民调数据，展示趋势变化
  - 用户可查看「2024 vs 2025」的态度分布对比

#### 8.2.3 真实用户标签数据

- 从 mock 用户过渡到真实用户填写的标签
- 分析真实标签分布，优化标签库

### 8.3 未来扩展：其他干预路径

#### 8.3.1 情绪过滤（Reiljan 2020）

- 在评论区添加「隐藏情绪化评论」开关
- 与 Path B（分布可视化）进行 2×2 实验对照

#### 8.3.2 去标签阅读（Flaxman 2016）

- 隐藏用户名、头像、点赞数等社交信号
- 仅展示评论文本本身

#### 8.3.3 跨国议题扩展（Path B 扩展）

- 添加中美跨国议题（气候、性别平等）
- 使用 Pew Global Attitudes / WVS 数据
- 展示中美公众态度重叠程度

### 8.4 未来扩展：产品化

- **真实用户系统**：从 mock 用户过渡到真实注册用户
- **个性化推荐**：基于用户兴趣推荐报道
- **移动端适配**：响应式设计 + PWA
- **实时评论系统**：Supabase Realtime 订阅

* * *

## 9. 成功标准

### 9.1 技术层面

**基础功能**：
* [ ] Feed 流流畅（无卡顿）
* [ ] 用户认证系统正常运行
* [ ] 评论系统正常运行
* [ ] 优雅降级（Supabase 未配置时回退 JSON）

**Path B（共识可视化）**：
* [ ] 直方图在 4–7 档数据下均正确显示
* [ ] 评论区顶部固定展示直方图
* [ ] 独立数据按钮展开直方图
* [ ] 暖色系中性配色正确应用

**Path C（交叉身份）**：
* [ ] 评论卡片正确显示用户标签
* [ ] 标签 chips 横向可滑动
* [ ] 点击有叙事的标签展开个人叙事
* [ ] 注册引导流程完整可用

### 9.2 研究层面

**Path B 验证**：
* [ ] 用户能理解直方图的含义
* [ ] 直方图与极端评论的并置能产生认知冲击
* [ ] 桥接文案有效消除突兀感

**Path C 验证**：
* [ ] 用户能自然发现与对立立场者的共同身份
* [ ] 标签展示不会产生操纵感
* [ ] 个人叙事展开增强共鸣效果

**可测量指标**：
* [ ] 评论按钮 vs 独立数据按钮的点击率
* [ ] 用户在直方图区域的停留时间
* [ ] 用户点击标签查看叙事的频率
* [ ] 可用于 A/B 测试验证假设

**体验层面**：

* [ ] 界面简洁、美观
* [ ] 直方图出现不突兀（桥接文案有效）
* [ ] 交互自然，符合 Twitter 风格用户习惯
* [ ] 卡片点击可正确跳转外部新闻源

* * *

## 10. 风险与应对

### 风险1：用户快速跳过直方图

**表现**：用户点开评论区后直接滚到评论，不看直方图

**应对**：

* 默认曝光设计本身已确保视觉接触——即使快速扫视，锚定效应仍然存在
* 可通过 A/B 测试量化"扫视 vs 不展示"两种情况下的感知极化差异
* 首次访问时可加轻量引导提示

### 风险2：直方图太复杂

**表现**：用户看不懂直方图

**应对**：

* 桥接文案用自然语言解释量表含义
* 采用水平柱状图（最直觉的图表形式）
* 内测时收集反馈，必要时简化标签措辞

### 风险3：数据质量

**表现**：Pew 原始数据的量表设计不统一（4档/5档/7档混存）

**应对**：

* 直方图组件设计为 N 档可变，不硬编码档数
* 每个议题独立维护量表标签和分布数据
* Demo 阶段仅选 2–3 个已确认数据可用的议题

### 风险4：用户认为平台有立场偏见

**表现**：用户觉得展示"大部分人态度温和"是平台在暗示极端立场是错的

**应对**：

* 直方图仅呈现数据，不附加任何价值判断
* 桥接文案保持中性描述（"态度分布如下"），不使用"实际上""真相是"等引导性措辞
* 明确标注数据来源和调查年份，增强可信度

* * *

## 附录A：参考文献

**核心理论**：

* Lerman, K., et al. (2016). The "majority illusion" in social networks. *PLOS ONE*.
* Moore-Berg, S. L., et al. (2020). Exaggerated meta-perceptions predict intergroup hostility. *PNAS*.
* Helberger, N. (2021). The political power of platforms: How current policy-making disregards the public. *Communications*.

**相关研究**：

* Milli, S., et al. (2024). Engagement, user satisfaction, and the amplification of divisive content. *PNAS Nexus*.
* Robertson, C. E., et al. (2023). Negativity drives online news consumption. *Nature Human Behaviour*.

* * *

## 附录B：快速启动命令

```bash
# 创建项目
npm create vite@latest majority-illusion-demo -- --template react-ts

# 安装依赖
cd majority-illusion-demo
npm install

# 启动开发服务器
npm run dev

# 打包部署
npm run build
```

* * *

**文档版本**: v2.0
**最后更新**: 2026-02-23
**作者视角**: HCI研究工程师 × 产品设计师