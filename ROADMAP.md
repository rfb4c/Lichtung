# Lichtung 开发路线图

> **项目定位**: 感知极化校准研究 Demo
> **当前版本**: v0.4.7
> **最后更新**: 2026-08-22

---

## 🎯 项目目标

### 研究背景
当今社会的**感知极化**现象日益严重，人们往往高估公众意见的极端程度。这种误判主要通过**媒体传播**被放大——极端观点获得更高曝光，而温和的大多数保持沉默。

### 项目性质
这是一个研究原型，用于展示如何通过**系统设计干预**来降低媒体环境中的感知极化程度。三条路径均为已实装的设计干预，效果尚未经对照实验检验。

### 干预路径
- **Path A (反刻板印象)**: 通过 Feed 排序干预，把反刻板印象内容提升到用户自然浏览范围内，校准接触到的样本结构（设计规范中的「高敌意内容降权」已转为排除方案，见设计规范 § 7 方案 E）
- **Path B (温和多数可视化)**: 通过可视化真实民调数据，修正用户对公众态度分布的感知，打破"所有人都站在两个极端"的误判
- **Path C (交叉身份)**: 通过展示交叉身份标签，软化政治对立群体间的边界，促进人性化理解

### 数据策略
- **当前**: 使用静态 JSON 数据展示核心功能
- **未来**: 扩展为自动新闻采集系统，实现数据动态更新

---

## 📅 版本规划

> ⚠️ **下方 v0.3.0 及之后的条目停留在 2026-03 的排期，尚未逐条重排。**
> 2026-03 之后的实际进展见「2026 年 3–8 月实际进展」一节与当前状态面板；
> 两者冲突时以那两处为准。

### ✅ v0.1.0 - 基础框架 (已完成)
**发布时间**: 2025-12-15
**里程碑**: 完成基础新闻流、用户系统、评论区

**核心功能**:
- ✅ Supabase 集成（用户认证、数据库）
- ✅ 新闻报道流（FeedItem）
- ✅ 评论系统（CommentSection）
- ✅ 个人资料页（ProfilePage）
- ✅ 深浅主题切换
- ✅ 媒体源 Logo（Apistemic API）

---

### ✅ v0.2.0 - Path B Phase 1 & 2 (已完成)
**发布时间**: 2026-03-07
**里程碑**: Path B 温和多数可视化核心功能上线

**Phase 1: 数据架构重构**
- ✅ 类型系统更新（Event → Topic, Distribution → PollingData）
- ✅ 删除旧设计（SpectrumBar, DistributionTooltip）
- ✅ 创建 app-data.json（topics, pollingData, reports）

**Phase 2: 组件实现**
- ✅ DistributionChart 组件（4-7 档可变直方图）
- ✅ CommentSection 集成（顶部固定图表）
- ✅ FeedItem 双按钮（单看数据 / 单看分析）
- ✅ 主题标签显示
- ✅ 基础主题匹配算法（matchers.ts）

**数据内容**:
- ✅ 3 个主题（Gun Control, Abortion, Climate Change）
- ✅ 真实民调数据（Pew Research）
- ✅ 24 篇示例报道

---

### 🚧 v0.3.0 - Path C 实现 (进行中 - 最高优先级)
**目标发布**: 2026-03-20
**里程碑**: 完成 Demo - 两条干预路径全部实装

#### 🔴 最高优先级 (CRITICAL - Demo 核心功能)

##### 👥 Path C: 交叉身份标签系统（静态原型）
🤔 **状态**: 🟢 完成 (2026-03-07)
**预估工时**: 4-7 小时（静态原型）
**优先级**: ⭐⭐⭐ 最高

**实施策略**: 与 Path B 一致，先用静态数据打通呈现，确认无误后再做数据库实装

**Phase 1: 静态数据原型** (✅ 已完成)
- [x] **扩展 `app-data.json`**
  - [x] 为示例用户添加 `identities` 字段和 `avatarUrl`
  - [x] 设计身份标签类别与结构（Family/Experience/Role/Lifestyle 四层模型）
  - [x] 12 个用户配置精细交叉标签，73 条定制评论覆盖 24 篇报道
  - [x] 添加对应的 emoji 和隐藏叙事 (Narrative)

- [x] **CommentItem 展示身份标签**
  - [x] 组件读取 `identities` 并渲染为可横向滚动的 Chip 列表
  - [x] 实现带叙事标签的蓝色指示点和点击内联展开功能
  - [x] 完善响应式布局，保证不干扰评论阅读

- [x] **视觉与 UI 调整**
  - [x] CSS Variables 适配浅色/暗色模式（暗色使用 #2f3336, 浅色 #e5e3dc）
  - [x] 添加高质量真人配图头强化社交真实感

**Phase 2: 用户个人页面系统** (🚧 进行中)
**状态**: 🟡 开发中
**预估工时**: 6-8 小时
**优先级**: ⭐⭐⭐ 最高

**核心功能**: 点击评论区头像 → 跳转用户个人页面，展示完整身份标签与历史评论

**Phase 2.1: 基础页面实现**（当前优先）
- [ ] **创建 UserProfilePage 组件**
  - [ ] 使用前端路由（URL hash）实现页面导航
  - [ ] 显示用户头像、名字、完整身份标签
  - [ ] 所有身份标签平铺展示（比评论区更突出）
  - [ ] 带 narrative 的标签可点击展开

- [ ] **评论历史列表**
  - [ ] 显示该用户的所有评论（按时间倒序）
  - [ ] 每条评论包含：内容、时间、所属报道标题
  - [ ] 报道标题可点击，跳转回该报道页面
  - [ ] 显示报道的主题标签（Gun Control / Abortion / Climate）

- [ ] **导航与交互**
  - [ ] CommentItem 的头像可点击
  - [ ] 返回按钮回到之前位置
  - [ ] 响应式设计（移动端 + 桌面端）

**Phase 2.2: 增强功能**（效果满意后）
- [ ] 按主题 Tab 切换查看（All / Gun Control / Abortion / Climate）
- [ ] 轻量级"人设概览"文本（自动从身份标签生成）
- [ ] Narrative 展开动画优化
- [ ] 页面过渡动画

**Phase 2.3: Supabase 集成**（🚧 当前优先）
**状态**: 🟡 规划中
**预估工时**: 4-6 小时
**优先级**: ⭐⭐⭐ 最高

**核心任务**: 将 ProfilePage 与 Supabase 认证系统集成，支持登录用户存储身份标签

- [x] **修改 Supabase Schema** ✅ 完成于 2026-03-08
  - [x] 在 `profiles` 表添加 `identities` 列（JSONB 类型）
  - [x] 创建索引优化查询性能
  - 📄 脚本: `scripts/supabase-add-identities.sql`

- [x] **更新类型定义** ✅ 完成于 2026-03-08
  - [x] `UserProfile.identities` 改为必填（非可选）
  - [x] `ProfileRow.identities` 更新为 `IdentityTag[]`
  - [x] `mapProfile` 默认值改为 `[]`

- [x] **更新 AuthContext** ✅ 完成于 2026-03-08
  - [x] 修改 `handleUpdateProfile` 支持 `identities` 字段
  - [x] 添加类型定义：`identities: IdentityTag[]`

- [x] **改造 ProfilePage** ✅ 完成于 2026-03-08
  - [x] 集成 `useAuth()` hook 检查登录状态
  - [x] 已登录：从 Supabase 读取/保存 identities
  - [x] 未登录：继续使用 localStorage Guest 模式
  - [x] 添加登录状态提示 UI (guestNotice 样式)

- [x] **导入 Mock 用户数据** ✅ 完成于 2026-03-08
  - [x] 将 `app-data.json` 中的 12 个 mockUsers 导入到 `profiles` 表
  - [x] 创建测试账号（12 个用户，密码: pass1234）
  - [x] 验证数据完整性（identities, avatarUrl, display_name）
  - 📄 脚本: `scripts/import-mock-users-simple.sql`
  - 📘 指南: `scripts/MOCK_USERS_IMPORT_GUIDE.md`

- [x] **测试完整流程** ✅ 完成于 2026-03-08
  - [x] 未登录状态：Guest User + localStorage
  - [x] 登录后：Supabase 用户 + 数据库持久化
  - [x] 编辑并保存身份标签
  - [x] 刷新页面数据保留
  - [x] 构建成功 (`npm run build`)

---

##### ✅ Phase 2.4: 评论数据导入（Supabase 集成）
**状态**: ✅ 已完成
**预估工时**: 2-3 小时
**完成时间**: 2026-03-08

**核心任务**: 将评论数据导入 Supabase，实现 ProfilePage 和 CommentSection 读取真实数据

- [x] **创建 Comments 表（如未创建）**
  - [x] 验证 `comments` 表存在并包含必要字段
  - [x] 字段：`id`, `report_id`, `user_id`, `content`, `created_at`
  - [x] 添加外键约束（`user_id` → `profiles.id`）
  - 📄 脚本：`scripts/verify-comments-table.sql`

- [x] **导入 Mock 评论数据**
  - [x] 将 `app-data.json` 中的 73 条 mockComments 导入到 `comments` 表
  - [x] 自动匹配 `userId` 到实际的 profile UUID
  - [x] 验证外键关联正确
  - 📄 脚本：`scripts/import-mock-comments.sql` (自动生成)
  - 🛠️ 生成工具：`scripts/generate-comment-import-sql.cjs`

- [x] **更新 ProfilePage 读取真实评论**
  - [x] 替换第 83-86 行的空数组返回
  - [x] 实现 Supabase 查询：关联 `comments` + `reports` + `topics`
  - [x] 支持优雅降级到 `app-data.json`
  - [x] 按时间倒序显示评论历史
  - 📄 文件：[src/components/ProfilePage.tsx](src/components/ProfilePage.tsx#L84-L168)

- [x] **CommentSection 已支持真实评论**
  - [x] 已从 Supabase 读取评论数据（Phase 2.3 已实现）
  - [x] 已保留静态数据降级方案（fallback）

- [x] **构建验证**
  - [x] TypeScript 编译通过
  - [x] Vite 构建成功
  - [x] 无运行时错误

**参考文档**:
- 📘 [导入指南](scripts/COMMENT_IMPORT_GUIDE.md)
- 📘 Supabase 数据库 schema 文档

**🎯 下一步**: Phase 3 - Path B 温和多数可视化功能实现

---

**Phase 3: 完全迁移到 Supabase**（v0.4.0 或更晚）
- [ ] 移除 localStorage 降级方案
- [ ] 删除 `profiles` 表的 `city`, `profession`, `interests` 旧字段
- [ ] 用户可自主注册并编辑身份标签
- [ ] 实现完整的 CRUD 操作

**参考文档**: `docs/02-Path-C-交叉身份/设计规范.md`

---

##### 📊 扩充示例数据（Demo 用）
**状态**: 🟡 可选
**预估工时**: 4-6 小时

- [ ] 再添加 1-2 个主题（Immigration / Healthcare）
- [ ] 每个主题 5-8 篇报道即可（Demo 展示用）
- [ ] 收集对应的真实民调数据

---

#### 🟢 中优先级（Demo 润色）

##### 🎨 UI/UX 增强
- [ ] 图表加载骨架屏
- [ ] 身份标签动画效果
- [ ] 暗色主题完善

##### 📱 移动端 / 平板端响应式修复
**状态**: ✅ 已完成
**分支**: `fix/mobile-responsive-v2`
**优先级**: ⭐⭐ 高

- [x] **卡片溢出修复** — `mainColumn` 改为响应式宽度，消除手机端横向滚动
- [x] **按键区左对齐** — `actions` 从 `space-between` 改为 `flex-start + gap`
- [x] **平板侧边栏** — 768-1023px 显示 X 风格图标侧边栏（68px），< 768px 保留底部导航
- [x] **UserMenu 平板适配** — 窄侧边栏下隐藏文字，仅显示头像

---

### 🔮 v0.4.0 - 部署上线 (计划中)
**目标发布**: 2026-03-25
**里程碑**: Demo 公开可访问

**核心任务**:
- [ ] 生产环境配置
- [ ] Vercel/Netlify 部署
- [ ] 域名绑定（可选）
- [ ] 性能优化（代码分割、图片优化）
- [ ] SEO 优化（Open Graph、meta 标签）
- [ ] 错误监控（Sentry 等）

---

### 🔮 v0.5.0 - News Agent 自动采集 (计划中)
**目标发布**: 2026-04-15
**里程碑**: 从静态数据到动态采集

**核心功能**:
- [ ] 新闻源爬虫（RSS/API）
  - [ ] 主流媒体 RSS 订阅
  - [ ] NewsAPI / MediaStack 集成
- [ ] 自动主题标注
  - [ ] 复用 `scripts/annotate/` 的适配器结构做语义分类
  - 注：原计划的关键词匹配器 `matchers.ts` 已于 2026-08 删除（28 篇报道全部硬编码
    `topicId`，匹配器见到就跳过，实际是死代码）。自动采集上线后要重新设计这一层
- [ ] 内容去重与过滤
- [ ] 定时任务调度（每日更新）

**参考文档**: `docs/03-News-Agent/设计规范.md`

**注**: News Agent 之后，主题匹配算法优化才成为必要

---

### 🔮 v1.0.0 - 研究就绪版 (计划中)
**目标发布**: 2026-05-01
**里程碑**: 可用于实验的完整系统

**核心功能**:
- [ ] 主题匹配算法优化（基于 News Agent 的实际数据）
  - [ ] ML-based 主题分类
  - [ ] 多主题支持
  - [ ] 匹配置信度
- [ ] A/B 测试框架
- [ ] 用户行为分析与数据导出
- [ ] 性能优化与压力测试

---

## 🔄 后期优化任务 (v1.0+ 阶段)

### 主题匹配算法优化
**优先级**: 低（News Agent 之后）
**说明**: 当前静态数据下，主题匹配不是必需的。只有启用 News Agent 自动采集后才需要。

- [ ] 完善 `matchers.ts` 关键词匹配
- [ ] 基于 ML 的主题分类（GPT-4 / 本地模型）
- [ ] 多主题支持（一篇报道匹配多个主题）
- [ ] 匹配置信度评分

### Supabase 数据迁移
**优先级**: 低（Demo 阶段不需要）
**当前方案**: 静态 JSON（app-data.json）

- [ ] 创建 `topics` 表
- [ ] 创建 `polling_data` 表
- [ ] 更新 `reports` 表 schema
- [ ] 数据迁移脚本
- [ ] 更新 App.tsx 使用数据库

### 分析与监控
- [ ] 追踪用户最关注的主题
- [ ] 追踪"单看数据" vs "单看分析"点击率
- [ ] A/B 测试不同桥接文本的效果
- [ ] Path B vs Path C 效果对比

---

## 🗓 2026 年 3–8 月实际进展

3 月之后的工作没有按上面的版本节奏走，集中在**让代码与文档的描述一致**上——
原型能跑不等于文档描述属实，而后者是能不能拿这个项目写东西的前提。

**v0.4.4（2026-08）· 整顿**

- 删除死字段与不自洽的数据流：`hostilityScore`、`contentType` 从类型与数据中移除；
  关键词主题匹配器 `matchers.ts` 删除（28 篇报道全部硬编码 `topicId`，匹配器实际不生效）
- 修正 README 里失实的干预对比声称；版本元信息对齐
- Path A 降权侧（Piccardi 原版）由「设计留档」改写为**已排除方案**：它针对情感极化，
  本研究针对感知极化，机制链路不同。排除理由本身是一个有价值的设计判断
- 配图溯源：新增 `docs/image-provenance.md` 与生成脚本，6 条张冠李戴的配图换成文章自身
  头图，28/28 实测可加载，图源与标注来源一致 22/28
- 仓库零配置可运行（不建 `.env` 也能跑）

**v0.4.5（2026-08-22）· Path A 打分管线与民调溯源**

- Path A 排序改为**连续 csScore 位移 + 间隔调整**，与设计规范一致；
  新增 `src/lib/csScore.ts` 作为加权公式的唯一实现
- 新增 `scripts/annotate/` **双评审标注管线**：两家厂商的模型各自独立读同一份判据，
  逐属性比对，分歧取半分（事前写定，不设人工裁决），加权写回 `csScore`。
  判据与 JSON Schema 随代码入库，两个 judge 的完整原始判定落在 `annotations.json`
- 设计规范新增 § 3.5 三属性判据（典型性 / 异质性 / 中等违背）与理论溯源
- `PollingData` 补溯源字段，图表显示**原始题干**与可点击来源；
  新增 `scripts/check-polling-consensus.mjs` 把民调入库判据变成可重跑的检查

**v0.4.6（2026-08-27）· 报道 → 民调 匹配管线**

- 新增 `scripts/match-polling/` **双评审匹配管线**：判定一篇报道的核心命题是不是
  某条民调正在问的那件事。判据是**命题对齐而非议题相同**，产出写回
  `report.pollingDataId`——此前该字段由人工填写，现在成为可审计的管线产出
- 裁决规则事前写定、无人工干预：两个 judge 都判 null 则不挂；落在同一「问题组」
  则挂该组最新一条；**其余一切情况（含一方 null、分属不同组）一律不挂**。
  与 Path A「分歧取半分」是同一种克制——挂错比不挂代价更高
- 年份不参与一致性判定：同题不同年的民调（2024/2026 两条堕胎合法性）归为一组，
  组内取哪条由确定性规则决定，不经过模型。这让此前完全未被使用的 2026 那条
  进入了可挂载范围
- schema 强制三项证据（报道原句 / 民调题干概念 / 对齐理由），只吐 id 不可审计
- 新增一道标注管线没有的闸：**候选外 id 即判调用失败**——候选逐篇不同，schema
  约束不了，编出来的 id 会在前端变成查不到的引用、图表静默消失

**v0.4.6 补记（2026-08-27）· 匹配管线首次实跑**

- 28 篇 × 2 judge = 56 次调用，零失败。两个 judge 的完整原始判定（含逐条证据字段）
  落在 `src/data/poll-matches.json`，任何人可据此自行重算裁决
- 裁决：一致挂载 8 / 一致不挂 15 / 分歧不挂 5，两个 judge 一致率 82.1%
- **信度**：Cohen κ = 0.632，95% CI [0.339, 0.924]；Gwet AC1 = 0.664；偏差指数 17.9%。
  n=28 使区间跨越「中等—几乎完美」三档，论文须连区间一起报，不可只写点估计
- **复现校验**：再跑一轮 56 次调用，逐条判定稳定 54/56 (96.4%)，裁决产出稳定 26/28 (92.9%)。
  两次挂载总数都是 8/28 但构成不同（1 增 1 减，方向相反）——只报总数会高估稳定性
- **judge 间存在方向性差异**：5 条分歧方向全部相同（宽松方挂、保守方不挂，0 条反向），
  保守方的挂载集合是宽松方的真子集——在这份数据上「分歧即不挂」等价于「由保守方单独决定」。
  但只有 5 个不一致对，McNemar 精确检验 p=0.0625，**未达 0.05**：应写「方向完全一致，
  样本量不足以统计上确立」，不可写「存在显著的系统性偏差」。双评审的制衡未被双向触发，
  这一点须在论文方法段如实声明
- 气候标杆组与人工判断 7/9 一致；两处不一致均为两个 judge 一致地反对人工判断，
  且理由指出人工判据在体裁一致性上存在内部矛盾
- **裁决结果尚未写回**：`app-data.json` 的挂载仍是人工填写的 11/28。
  写回是不可逆的观感改动（6 条现有图表会消失），留待人工核对后决定

**v0.4.7（2026-08-29）· 卡片文本改用出版方原文，判定输入改为槽位模型**

- **撰写摘要从系统里消失。** 卡片摘要此前由本项目撰写——卡片挂着真实署名与 URL，
  一段我们自己写的话安在出版方名下，是完整性问题而非措辞问题。改为取出版方自己写的
  `og:description`（缺失退到正文首段），从**带时间戳的 Wayback 快照**取回，
  36 篇里 29 篇用 og、3 篇用首段、4 篇存档上确实没有可用文本、如实保留并标注
- 新增 `Report.summaryProvenance` 字段：这段文字出自出版方还是出自我们，
  写在数据层而不是只写在文档里，读者与审阅者都能当场查到
- 新增 `scripts/harvest-source-texts.py`：按响应头解压、在 DOM 上剪掉非正文容器、
  按容器打分定位正文。产出 `src/data/source-texts.json`（36 条，正文合计 3.0 万词，
  剔除 208 段站点样板），每条带快照 URL 与 14 位时间戳
- **判定输入改为槽位模型**：judge 读到的是标题 + 全部非空槽位（og / 首段 / 正文），
  抓到什么给什么。输入深度因此是**被记录的结果**而不是事前设的门槛——
  逐条层级落在产物的 `tiers` 里，供分层报告一致率
- **新增「证据不足」弃权**，与「判定为不成立」分开记录。打分上同权（都不干预），
  统计上分开：两个 judge 都弃权则剔除出 κ 的分母并单独报弃权率，
  一方弃权算作不一致。后者是真实分歧——剔除它等于把最难的条目从考卷上划掉
- Path B 的 Judge A 由 `claude-opus-5` 改为 `claude-sonnet-5`，与 Path A 一致：
  仓库里找不到任何为这条不对称辩护的记录，它是早期「两个 Claude 当双评审」方案的残留
- 语料 28 → 36，移民议题补 9 篇，四议题各 9 篇。移民 9 篇中 NPR 占 4，
  且 4 篇是法院/机构裁决（无个人主角）——两条偏移都要写进论文，不是要修的缺陷
- **两条管线的已有产物全部作废**，需按新判据与新语料重跑；哈希守卫已把
  `source-texts.json` 一并纳入，判定输入变了而没重跑会被拒绝续跑

---

## 📊 当前状态面板

| 功能模块 | 状态 | 说明 |
|---------|------|------|
| Path B 温和多数可视化 | ✅ 已实装 | 按需展开的分布图表层。**覆盖不全**：4 个议题只有 3 个有民调（移民已确认无可用数据） |
| Path C 交叉身份 | ✅ 已实装 | 常驻的身份标签展示层，无开关 |
| Path A Feed 排序干预 | ✅ 已实装 | 连续 csScore 位移 + 间隔调整，可在界面切换排序模式 |
| Path A 标注管线 | 🟡 待重跑 | 代码已改为槽位输入 + 弃权判定；`annotations.json` 是 28 篇旧语料、旧判据的产物，已作废 |
| 民调库扩展 | 🟡 4 条入库 | 判据、字段、图表、校验脚本均就绪；3 条按判据排除并留档。扩库等人工核实的新数据 |
| 报道→民调匹配管线 | 🟡 待重跑 | 同上，另 Judge A 由 opus 改为 sonnet；`poll-matches.json` 已作废 |
| 出版方原文采集 | ✅ 36/36 | `source-texts.json`：og 30 / 正文 32 / 只有标题 4，每条可追溯至带时间戳的存档快照 |
| 卡片摘要 | ✅ 32/36 出自出版方 | 29 篇 og:description、3 篇正文首段；4 篇存档无可用文本，保留撰写摘要并在数据层标注 |
| 配图溯源 | 🟡 28/36 | 原有 28 篇实测可加载；新增 8 篇移民报道缺配图，存档快照里已抓到 og:image，待人工过版权与署名 |
| Demo 部署 | 🔵 未开始 | — |
| News Agent | 🔵 未开始 | — |

**图例**: ✅ 已实装 | 🟡 进行中 | 🔵 未开始 | 🔴 受阻

**当前焦点**: 冻结 `app-data.json` / `source-texts.json` / 判据 / schema，
用新语料与新判据重跑两条管线。

**待人工核**: 36 篇里 22 篇的标题与存档快照的 `og:title` 不一致——部分只是站点后缀，
部分是实质改写（`rp_gun_001` 连伤亡数字都不同）。标题是关于出版方的事实主张，
`npm run backfill:summaries` 会把逐条对照打印出来，但**不会自动改**。

**口径红线**：系统状态一律写 built / implemented，**不写 validated / tested / effective**——
三条路径的干预效果均未经对照实验检验。`engagementScore` 是人工填写的模拟参数，
不对应任何真实互动数据。

---

## 📝 工作流程规范

### 新增功能/任务时
1. 在 `ROADMAP.md` 对应版本下添加任务条目
2. 更新 `CLAUDE.md` 的当前任务列表
3. 如有设计变更，更新 `docs/00-Overview/设计更新日志.md`

### 完成任务后
1. 在 `ROADMAP.md` 中标记为完成（✅）
2. 更新 `CLAUDE.md` 移除已完成任务
3. 更新相关文档的实施计划
4. 提交代码并创建 PR

### 版本发布时
1. 更新 `package.json` 版本号
2. 在 `docs/00-Overview/设计更新日志.md` 记录版本变更
3. 在 `ROADMAP.md` 标记版本为完成
4. 创建 Git tag

---

## 🔗 相关文档

- [产品设计文档](docs/00-Overview/产品设计文档.md) - 研究动机与核心功能
- [技术设计文档](docs/00-Overview/技术设计文档.md) - 系统架构与数据库设计
- [设计更新日志](docs/00-Overview/设计更新日志.md) - 版本迭代历史
- [Path B 实施计划](docs/01-Path-B-温和多数可视化/实施计划.md) - Phase 1-4 详细步骤
- [CLAUDE.md](CLAUDE.md) - AI 助手工作上下文

---

**最后更新**: 2026-03-07
**当前分支**: `feature/path-b-consensus-visualization` → 待合并到 `develop`
